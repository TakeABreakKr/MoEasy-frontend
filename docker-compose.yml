# 개발환경에서 임시로 사용할 목적으로 작성됨

services:
  next-server:
    build: .
    image: next-server
    container_name: next-server
    ports:
      - 4000:4000
    extra_hosts:
      - "host.docker.internal:host-gateway"
